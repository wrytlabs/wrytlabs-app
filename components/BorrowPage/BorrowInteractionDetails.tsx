import AppBox from '@components/AppBox';
import AppCard from '@components/AppCard';

export default function BorrowInteractionDetails() {
	return (
		<AppCard>
			<span className={`font-bold text-xl`}>Outcome</span>

			{/* show whitelisted addresses */}
		</AppCard>
	);
}
